# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# tried by MM; it did not succeed to build the doc, which fails with info: You are in 'detached HEAD' state.
# advice.detachedHead: false

# Set the OS, Python version and other tools you might need build:
build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    # You can also specify other tool versions:

# Build documentation in the "docs/" directory with Sphinx
sphinx:
  builder: html
  configuration: doc/source/conf.py
  fail_on_warning: false

# Optionally build your docs in additional formats such as PDF and ePub formats:
#    - pdf
#    - epub

# Optional but recommended, declare the Python requirements required
# to build your documentation
# See https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html
# python:
#    install:
#    - requirements: docs/requirements.txt
